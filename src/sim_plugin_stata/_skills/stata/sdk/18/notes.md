# Stata 18 notes

The patterns in `base/` work unchanged on this version. pystata ships at
`<install>/utilities/pystata`; `config.init('mp'|'se'|'be')`.

This file exists so the layer is non-empty and the skills layout check passes.
